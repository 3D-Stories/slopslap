# v0.2 epic/issue stack — for adversarial review

Review this stack ADVERSARIALLY: ordering, dependencies, and whether any issue should be SPLIT (too
big / mixes concerns) or COMBINED (too granular / artificial seam). Flag missing work, over/under-scope,
and wrong dependency edges. Push back on the ordering.

## Context
slopslap (a Claude Code prose-repair plugin) shipped v0.1.0: deterministic mechanics + guards + a FROZEN
demonstration. The eval only proves behavior via seeded candidate edit-scripts — the **live,
model-in-the-loop** path is not wired. This epic (#16) closes that gap.

Existing, DONE + tested (don't re-litigate): fixtures + two-stage hard-gate runner; scanner (11 metrics,
vendored CommonMark); invariant ledger schema + Layer-1/2 verify + decision rule; backup + selective-
apply ENGINE; 3 canonical fixtures + 2 clean controls (COMPLETE).

## The stack (epic #16), current order + one-line scope

1. **#17 semantic-verify** — a real fresh-context Layer-3 `semantic_fn` (separate model invocation;
   receives only original+revision+ledger), wired into `verify` + exercised. Today Layer 3 is a stub →
   every verify defaults to SURFACE, ACCEPT unreachable.
2. **#18 protected-span auto-extractor** — extract code/inline-code/URLs/blockquotes/identifiers from an
   ARBITRARY doc into `protected_spans[]` (reuse the scanner tokenizer). Today: hand-authored only.
3. **#19 invariant-ledger auto-build** — whole-doc auto-extraction of invariant entries (numbers/units/
   dates/modals/negation/conditions/defined-terms) for arbitrary prose. Today: `build_ledger` needs
   hand-declared regions.
4. **#20 live passage-local locality** — derive authorized edit ranges from the diagnoses; feed the
   verifier so locality is deterministically enforced live (today only enforced with pre-supplied ranges).
5. **#21 wire the apply command to the apply engine** (+ write-strategy hardening: hardlink fail-closed,
   metadata policy, symlink no-follow, EXDEV abort, fsync-in-prod, rewrite the "in-place" spec prose to
   "backup-first, staged, verified, atomic replace"). Engine is built+tested; command is disabled.
6. **#22 genre classifier** — classify (declaration > repo/file context > structural > content;
   low-conf → most preservation-heavy profile) AND make genre actually constrain diagnosis/verify
   (spec→preserve parallelism/normative vocab; personal→voice-weighted; PRD→challenge adjectives-as-reqs,
   don't vision-police). Today genre is inert prose.
6. **#23 suggest→deterministic-verifier wiring + a live audit/suggest output-shape golden** — suggest's
   invariant-check is currently model-reported; wire it to the real verifier. Plus one live end-to-end
   golden asserting the typed-diagnosis-record + focused-diff output shape.
7. **#24 manual voice sample** (one-shot, no learning) — accept an inline voice sample; bias diction
   among ALREADY-SAFE phrasings only, per the authority order; no persistence/hook (v2).
8. **#25 scanner threshold calibration** — calibrate soft-flag thresholds against a reference corpus
   (an AI-slop before/after corpus was just gathered into `research/ai-slop-corpus/`). Metric COUNT is
   done (11); this is the reliability half.

Plus a pending action the owner just approved: **fold the gathered corpus pairs into eval fixtures /
judge-trial data / calibration corpus** (respecting: don't reward invented specifics — the "simulation"
no-fabrication rule; licensing — only Wikipedia CC-BY-SA + MIT verbatim, blogs fair-use-quote-only;
direction mismatch in datasets; voice-discontinuity has NO prior art). Not yet an issue — should it be?

## Proposed leverage ordering (as listed): 17 → 18 → 19 → 20 → 21 → 22 → 23 → 24 → 25
Rationale: semantic verify + auto-extraction (17/18/19) unblock a genuine live audit; then live locality
(20), apply wiring (21), genre (22), suggest-wiring+golden (23), voice (24), calibration (25).

## Questions
1. Is the ordering right? What must precede what (real dependency edges), and where is the proposed
   order wrong? (e.g. does #23's "live golden" actually depend on 17/18/19 all landing first?)
2. Split candidates: is **#23** two things (verifier-wiring vs the live golden)? Is **#21** two things
   (command wiring vs the write-strategy hardening)? Is **#22** two (classifier vs genre-constrains-
   behavior)? Is **#17** honestly one issue or does it need a harness/contract sub-piece first?
3. Combine candidates: are **#18 + #19** (both "auto-extract from arbitrary input") really one issue?
   Are **#20 + #23** coupled enough to merge?
4. Missing work: is there an unlisted prerequisite — e.g. a single "live orchestration" seam that
   actually invokes the SKILL end-to-end (the thing that makes audit/suggest run at all), without which
   17–24 are components with no assembler? Should the corpus fold-in be its own issue?
5. Over/under-scope: any issue too big to land in one PR (>~500 lines / distinct phases), or so small it
   should be a checklist item on another?
