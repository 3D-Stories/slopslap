"""Corpus provenance: manifest schema + family-level disjoint split (#30).

The provenance manifest is the authoritative licensing + lane + split record for the
before/after corpus; #25 (calibration) and #28 (e2e golden) import from here.
"""
