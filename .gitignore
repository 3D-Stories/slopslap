__pycache__/
*.pyc
.pytest_cache/
.slopslap/
.venv/
*.egg-info/

# slopslap review-stage CLI artifacts (written to CWD; never committed)
findings.json
decisions.json
review.html

# UAT candidate docs: internal briefings, deliberately never published (repo is public).
# tests/test_genre.py skips its candidate-file tests when this dir is absent (#98).
docs/uat/candidate-test-files/
